<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Library module: holds native runtime binary, glibc support assets, and probe build.
         The companion APK (com.opencode.android.runtime) is deprecated — use in-process
         runtime via the main app's OpenCodeService instead. -->
</manifest>
